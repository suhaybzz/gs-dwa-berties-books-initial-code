<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Bertie’s Book Shop</title>
</head>
<body>
    <h1>Bertie’s Book Shop</h1>

    <h2>Available Books</h2>
    <ul>
        <% availableBooks.forEach(function(book) { %>
            <li>
                <strong><%= book.name %></strong>
                – £<%= book.price %>
            </li>
        <% }); %>
    </ul>

    <br>
    <a href="/">Back to home</a>
</body>
</html>
